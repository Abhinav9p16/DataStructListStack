/*package com.cg.linkedlist;

import org.junit.Assert;
import org.junit.Test;

public class MyNodeTest {
    @Test
    public void given3nosWhenLinkedShdPassTest() {
        MyNode<Integer> myFNode = new MyNode<>(56);
        MyNode<Integer> mySNode = new MyNode<>(30);
        MyNode<Integer> myTNode = new MyNode<>(70);
        myFNode.setNext(mySNode);
        mySNode.setNext(myTNode);
        boolean result = myFNode.getNext().equals(mySNode) && mySNode.getNext().equals(myTNode);
        Assert.assertTrue(result);
    }
}*/
